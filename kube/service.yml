apiVersion: v1
kind: Service
metadata:
  name: conscoord-client-service
  namespace: kgb-conscoord
spec:
  selector:
    app: conscoord-client
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
